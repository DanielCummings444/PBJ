version: "3.8"

services:
  openclaw-toolkit:
    build: .
    container_name: openclaw-toolkit
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - openclaw-data:/data
    # Allow host networking for Ollama access
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  openclaw-data:
